1

CREATE TABLE employee(
emp_no NUMBER(10) PRIMARY KEY,
emp_name VARCHAR(15) NOT NULL,
salary NUMBER(10) NOT NULL, 
address VARCHAR(15) NOT NULL, 
gender VARCHAR(1) 
CHECK(gender = 'M' or gender = 'F') NOT NULL);


2

CREATE TABLE department(
dept_no NUMBER(10) PRIMARY KEY, 
dept_name VARCHAR(15) UNIQUE);


3

ALTER TABLE employee ADD(d_no NUMBER(10), FOREIGN KEY (d_no) REFERENCES department(dept_no));


4

INSERT INTO Department (dept_no, Dept_Name, Location) VALUES (1, 'cse','delhi');

INSERT INTO Employee (Emp_No, Emp_Name, Gender, Salary, Address, D_No) VALUES (1, 'bob', 'M', 70000, 'delhi street', 1);

INSERT INTO Department (Dept_No, Dept_Name, Location) VALUES (2, 'ece','kochi');

INSERT INTO Employee (Emp_No, Emp_Name, Gender, Salary, Address, D_No) VALUES (2, 'dave', 'M', 30000, 'kochi street', 2);

5

INSERT INTO Employee (Emp_No, Emp_Name, Gender, Salary, Address, D_No) VALUES (2, 'dave', 'M/F', 30000, 'kochi street', 2);

6

DELETE FROM department WHERE dept_no=2;

DELETE FROM department WHERE dept_no=2;



7

ALTER TABLE employee DROP CONSTRAINT fk_dno;
ALTER TABLE employee ADD CONSTRAINT fk_dno_cascade FOREIGN KEY(d_no) REFERENCES department(dept_no) ON DELETE CASCADE;

8

---

9

SELECT NAME,DEPT_NAME
FROM STUDENT;

10

SELECT NAME FROM INSTRUCTOR WHERE DEPT_NAME='COMP. SCI.';

11.

SELECT TITLE
FROM COURSE WHERE CREDITS=3 AND DEPT_NAME='CSE';

12.

SELECT COURSE-ID,TITLE
FROM COURSE 
WHERE STUDENT.ID=12345;

13.
SELECT *
FROM INSTRUCTOR WHERE SALARY BETWEEN 40000 AND 90000;

14.
SELECT ID
FROM INSTRUCTOR,TEACHES
WHERE INSTRUCTOR.ID != TEACHES.ID;

15. 
SELECT STUDENT.NAME, COURSE.TITLE, TAKES.YEAR
FROM STUDENT,COURSE,TAKES,SECTION
WHERE SECTION.ROOM_NUMBER=303;

16.
SELECT STUDENT.NAME, COURSE.ID

17.
SELECT T.NAME, SALARY
FROM INSTRUCTOR T, INSTRUCTOR S
WHERE DEPT_NAME='CSE' AND T.SALARY>S.SALARY;

18.
SELECT NAME
FROM INSTRUCTOR WHERE DEPT_NAME LIKE '%CH%' ;

19.
SELECT NAME,LENGTH(NAME) AS LENGTH
FROM STUDENT;

20.
SELECT DEPT_NAME
SUBSTR(DEPT_NAME 3,3) AS SUBSTRING FROM DEPARTMENT;

21.
SELECT NAME ,UPPER(NAME)
FROM INSTRUCTOR;

22. 

23.
SELECT SALARY, ROUND(SALARY/3) AS SALARY BY 3
FROM INSTRUCTOR;

24.
SELECT 




